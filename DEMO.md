# Demo shot list (5 minutes at most; Hedera and Arc both cap at five)

Record once. One take per shot, cut together. Every shot ends on a link the judge can click. Since 2026-09-12 the gateway and the web are hosted on AWS ([RUN.md](RUN.md#6-hosted-on-aws-testnet): web https://dr7shuhqdeo3p.cloudfront.net, gateway https://d32gga9078v6q0.cloudfront.net), so T1 and T3 are already running there, the browser shots use the CloudFront URL, the fresh index is made on the box, and only T2 (seed) and T4 run on the laptop, both of which target the hosted gateway through `GATEWAY_URL` in `.env`. For a fully local take, the original sequence still holds. Setup before recording, exactly the sequence in [RUN.md](RUN.md) section 3: fresh index, T1 `pnpm gateway`, T2 `SEED_RUN=demo-<date> SEED_STAKE_USDC=10 SEED_ONCHAIN_SKILLS=cloud-helper pnpm seed` (needs 10 USDC in the deployer; claim ids are deterministic and the label is part of the nonce, so any label not used on this `SigilStake` before gives a fresh escrow on the same contract, no new deployment), T3 `pnpm web`, then roll; T4 runs as `E2E_FRESH_PAYER=1 POLICY_MIN_STAKE_USDC=10 POLICY_MIN_CLAIM_AGE_SEC=0 pnpm e2e`: `E2E_FRESH_PAYER=1` creates a throwaway Hedera payer (5 HBAR from the operator) and hands it 2 USDC from the agent account `0.0.10483155` (which holds 14 USDC; the hand-off needs at least 2), so the `402` lands on cloud-helper and is paid on the HTS USDC leg instead of being served free by the agent's licence NFTs; the two `POLICY_*` knobs mean there is no 60 s wait; and it runs with `AGENT_WALLET_BACKEND=circle`, `CIRCLE_WALLET_ADDRESS` and `CIRCLE_CHAIN=ARC-TESTNET` in `.env` plus a VALID testnet session in `circle wallet status` (RUN.md step 6), so its Arc transactions come from the Circle Agent Stack wallet, which needs 2.5 USDC for the bond (`circle wallet fund --address … --chain ARC-TESTNET` drips 20). Exact log lines and screen labels are in RUN.md section 3, and the reset between takes (refill the deployer from the Circle wallet, fresh index, `SEED_RUN=demo-<n>`) is its `Reset for another take` paragraph; `<cloud-helper>` below is the skill id `pnpm seed` prints for it (`be1d8306…`) and `<claim>` the LIVE claim the index holds when you roll (today's `demo-5` index on the hosted gateway: `0x08ee7465…`, escrow [0x0fd098fd…](https://testnet.arcscan.app/tx/0x0fd098fdb32b12e83ab9e8ea617325feb514d87724f5d3b81d2ec3d0a90aa2be), topic [0.0.10508627](https://hashscan.io/testnet/topic/0.0.10508627)). The links in the table are from the 2026-09-12 runs (shot 1 from the USDC-leg e2e run on the `demo-1` index and, for the payment and licence that appear on the skill page, the second USDC-leg run on the `demo-2` index; shot 2 from the first on-chain run; shot 3 from the Circle-wallet run on claim `0x88a529a0…`); a fresh recording produces its own.

| # | Time | On screen | Proves |
|---|---|---|---|
| 0 | 0:00 to 0:35 | README first line: "participants stake USDC and lose it when the evidence goes against them". `http://localhost:3000/`, the landing page: the masthead, then `Live from the gateway` with `skills listed 6`, `USDC escrowed on Arc 10.000000` in seal red, `claims broken 0`, `paid requests 0` and `last settlement 0xc2547b6b… off-chain record · HCS seq 3` (json-pretty's seeded settlement, polled every 10 s); scroll past `How a claim dies` to `What is real` (say it: four links a judge can click and one labelled mock). `Open the registry` → `http://localhost:3000/registry`: six rows under `10.000000 USDC escrowed on Arc across 6 skills · +40.000000 recorded off-chain`, `unstaked-echo` dimmed `no capital at risk`, `json-pretty` with `—` under `escrowed on Arc, USDC` and `10.000000` under `off-chain record, USDC`, stamp `RECORDED`, and `0 / 1` in `disputes sustained / total` (seed disputed its first 10 USDC claim with a benign trace and lost: click through to `/skills/<json-pretty>` for the claim `UPHELD`, the dispute `REJECTED`, resolution `CLAIM_UPHELD`, `off-chain record`; say that only escrowed capital counts as at risk), `cloud-helper` at `10.000000` escrowed in seal red, stamped LIVE and undisputed, `0` under `paid` on every row, `mock Selfie Check` in the top bar (say it is a stub); the contract address `0x66fc6324…1566fe` is the last row of the landing page's `What is real` ledger (say it is live on Arc testnet). Click `cloud-helper`: `/skills/<cloud-helper>`, the header `10.000000 USDC escrowed on Arc`, the LIVE claim `<claim>` in the claims ledger with its `escrow` link to the `openClaim` tx on Arcscan; then `Use this skill` (label `Access`): the price line `0.001000 USDC per KB · this source is 1 KB → 0.001000 USDC or 0.00100000 HBAR per request, settled by Blocky402 on hedera:testnet, paid to 0.0.10454761`. Press `Request the source`: the browser calls `GET /skills/<id>/source` with no licence and no payment and the `402 Payment Required` ledger appears, `USDC · HTS 0.0.429274` `0.001000 USDC` `1000 base units` and `HBAR` `0.00100000 HBAR` `100000 tinybar`, both to `0.0.10454761` on `hedera:testnet`, with the note that a browser cannot pay it and that `pnpm buy` does under it. Say it: a browser cannot pay this; the agent signs a partial Hedera transaction and Blocky402 submits it. `Licence holders 5` (`#22`, `#17`, `#16`, `#15`, `#3`, on the chain from earlier runs), `Payments 0` with `No paid request on this index yet. Run the agent below.` Leave the tab open. | The economics, not scanning; the failed outcome is already on screen before the live run adds the successful one; the paywall is live and the browser hits it; stubs are labelled |
| 0b | optional, 20 s (take it from shot 3) | Browser: `/skills/new` (`Register a skill` in the top bar). Pick `git-status` from `Start from an example`: the source fills in, the id under it is `sha256` of the code (say it: the id is the code). Press `Register`: three ledger lines, the id, `hcs://1/<topic>` on HashScan, `SKILL_REGISTERED`. Press `Open a claim on it`: the skill is preselected, choose `NO_CHILD_PROCESS` (say it: no allowlist, the sandbox never lets a subprocess run, so the bet is that this skill never tries; it shells out to git, so a 10 USDC stake here is about to be lost), Mock Selfie Check, `Stake 10.000000 USDC on Arc`, two wallet confirmations, the claim is LIVE | Anyone lists a skill for free; money enters only with a claim, on one of six machine-checkable rules |
| 1 | 0:35 to 1:25 | T4 `E2E_FRESH_PAYER=1 POLICY_MIN_STAKE_USDC=10 POLICY_MIN_CLAIM_AGE_SEC=0 pnpm e2e`. First two lines: `e2e: fresh payer 0.0.… (5 HBAR, no license yet)` and `e2e: fresh payer funded with 2 USDC (HTS 0.0.429274) from 0.0.10483155 <HashScan>`. `── policy ── install iff escrowedOnArc ≥ 10 USDC ∧ …`; per skill: `unstaked → skip` for `unstaked-echo`, `no capital escrowed on Arc (off-chain records only) → skip` for the four off-chain records, `cloud-helper … → install`. `── pay ──`: `x402: 402 Payment Required → signing partial Hedera tx`, `x402: paying the USDC leg (HTS 0.0.429274)`, `x402: signed → retrying with PAYMENT-SIGNATURE (Blocky402 submits, pays gas)`, `x402: settled · hedera:testnet · tx 0.0.7162784@…`, the HashScan line, `license NFT 0.0.10483154: minted to <payer> (mirror node confirms)`. Click the HashScan link (recorded run: [0.0.7162784@1789216091.354310097](https://hashscan.io/testnet/transaction/0.0.7162784@1789216091.354310097)): the mirror node shows `token_transfers` of 1000 base units of `0.0.429274` from the payer to `0.0.10454761` and the fee charged to Blocky402's `0.0.7162784`; say it: 0.001 USDC of HTS USDC per KB, the facilitator pays the gas. Back in the skill tab: within 10 s `Payments` shows the row (the payer, `0.001000 USDC`, `1.1 KB`, the settlement id with its HashScan link) and its `licence` column fills with the new serial once the mint lands; `Licence holders` is one longer (recorded runs: on the `demo-2` index payer `0.0.10502248`, [0.0.7162784@1789220863.825174533](https://hashscan.io/testnet/transaction/0.0.7162784@1789220863.825174533), serial 16; on the `demo-3` index payer `0.0.10502681`, [0.0.7162784@1789222346.496781431](https://hashscan.io/testnet/transaction/0.0.7162784@1789222346.496781431), serial 17). Why the fresh payer: the agent account `0.0.10483155` already holds licences for every staked fixture (serials 1, 3, 4, 9, 13, 14), so a plain `pnpm --filter @sigil/agent once` prints `x402: served free — license NFT already held, no 402` and never shows a `402`; the throwaway payer puts the `402` on cloud-helper itself, and the 2 USDC hand-off (skipped, HBAR paid instead, if the agent account holds less than 2 USDC) puts it on the USDC leg. | Hedera req 1 and 2: live x402 through Blocky402, one real paid request end to end; per-call metering; HTS USDC and the HTS licence NFT in the settlement path |
| 2 | 1:25 to 2:00 | Same terminal: `── install ──`, `sandbox: 2 inputs — benign + malformed, because error paths are where secrets leak`, `benign: 0 violation(s)`, `malformed (error path): 1 violation(s) — env:SIGIL_CANARY_AWS`. | Deterministic evidence; no model adjudicates |
| 3 | 2:00 to 2:50 | Same terminal (claim id from the recorded Circle-wallet run on `0x88a529a0…`; the same cycle ran again on `0x565446a0…` inside the shot-1 e2e run, [dispute](https://testnet.arcscan.app/tx/0xd7a487f785d0b6a86283fb4e51e7b6d628a0b1cace3a56137b839b3980062c23), [resolve()](https://testnet.arcscan.app/tx/0x05489e9d3c65259044e9d13687c19dd27c07ad62e78fc6a90115e1a853b3d9b4); the second USDC-leg run broke `0x68e1723e…` the same way, [dispute](https://testnet.arcscan.app/tx/0xaeb0e681131f937782724aa12f07c2c229561b31bc495baab84fa9693c93c1b4), [resolve()](https://testnet.arcscan.app/tx/0xe342487cbd645bdaabf3982b6a0caa2427d71c244d3b29a0a4ba4c653c6d2147), , the third broke `0xc6c24569…`, [dispute](https://testnet.arcscan.app/tx/0xa14ef33482f797b1644578d485a41275b4b9af9450a568352b73815373f7363d), [resolve()](https://testnet.arcscan.app/tx/0x9740ae00ab0f0e8e6aaa1b77707f7165e33dc4ba719eaa365993829c504bad90), and the fourth, run against the hosted gateway, broke `0x056f1a87…`, [dispute](https://testnet.arcscan.app/tx/0x6f1b2b77931afe400c109f710ca7cf87cf951a939ed0247a5b5731cf1c1d96ef), [resolve()](https://testnet.arcscan.app/tx/0xf9d4506fb3ac405769860e3ab4c34321e72a1f54a4cd7e2f3eeca04cdad5c1f1); today's index holds `0x08ee7465…`, a fresh seed prints its own). Point at the run's second line, `── wallet ── circle (Agent Stack wallet) 0xf6ede0518f7543715322cf7bd420aa6a7732b2e0 · Arc chain 5042002` (say it: every Arc transaction the agent sends is a `circle wallet execute` user operation from its Circle Agent Stack wallet). Then `── dispute ── claim 0x88a529a0… NO_ENV_READ_OUTSIDE · 1 violation(s) · traceHash 42ec4684…`, `stake 10.000000 USDC → counter-bond 2.500000 USDC from circle (Agent Stack wallet) 0xf6ed…`, `agent USDC before: 20.000000`, `circle wallet execute approve(address,uint256) 0x66fc… 2500000 --contract 0x3600… --address 0xf6ed… --chain ARC-TESTNET --rpc-url … --output json` and under it `approve(SigilStake, 2.500000)` [Arcscan](https://testnet.arcscan.app/tx/0x0e572df3a897fb5ea5f2b3fd8066fd24deada3c0df34555ce68e67c011b1e40c), `circle wallet execute dispute(bytes32,uint256,bytes32) 0x88a5… 2500000 0x42ec… --contract 0x66fc… …` and `dispute(claimId, bond, traceHash)` [Arcscan](https://testnet.arcscan.app/tx/0x36b8daad95a9af633be58651b469c17848329bbd854e53834e968c127b2188d2), `HCS DISPUTE_OPENED · evidence hcs://1/0.0.10500282 · traceHash …`, `── resolve ── verifier re-ran the bundle: {"reproduced":true,…}` (T1 shows `[verdict] … posted to the dispute resolver` with the [submitVerdict tx](https://testnet.arcscan.app/tx/0x17d23cf98fbe1274d666124913873fff553b63e3de280450c7ce5baf409f1952)), `circle wallet execute resolve(bytes32) 0x88a5… --contract 0x66fc… …` and `resolve(claimId)` [Arcscan](https://testnet.arcscan.app/tx/0xcef9eec96906eb76f0cef2c48196cb94551f5bf5862632f36e46efdaa8a6c73c), `── settled ── CLAIM_BROKEN · winner 0xf6ede0518f7543715322cf7bd420aa6a7732b2e0 (this agent)`, `payout 12.500000 USDC`, `agent USDC 20.000000 → 30.000000 (+10.000000)`. Open the `resolve()` tx on Arcscan: 12.5 USDC leaves `SigilStake` for the Agent Stack wallet, and the transaction itself went bundler → EntryPoint `0x0000000071727de2…` (an ERC-4337 user operation; the wallet paid no gas, which is why the delta is exactly the pot minus the bond). | Arc req 4 and 5: an Agent Stack wallet signs the agent's on-chain actions; the agent puts its own capital behind a real signal and is paid on-chain; the verifier reproduces and posts the verdict |
| 4 | 2:50 to 3:40 | Browser: `/skills/<cloud-helper>` shows, under `Use this skill`, the payment row with its licence serial and one more licence holder, then the claim BROKEN, the dispute SUSTAINED with the Agent Stack wallet `0xf6ed…` as disputer and its `bond on Arc` link, and the resolution with its `Arcscan` link; click `Money` to `/claims/<claim>/dispute?skill=<cloud-helper>` (recorded run: `/claims/0x88a529a0…/dispute`). Money panel: the `staker` column `0.000000` struck through, the `disputer` column `12.500000` with `+10.000000 from the other side`, the `pot 12.500000 USDC → disputer` line under the disputer, the `CLAIM BROKEN` stamp with `reason CLAIM_BROKEN`, `winner 0xf6ede051…32b2e0`, `settlement <resolve tx> on Arcscan · HCS seq 3` (the panel prints the run's own `arcTxHash`; the local-signer cycle showed `0x49b0b9b7…1cdd02`). To show the transition live instead, open this page before shot 3 (it polls every 3 s): the pot line slides under the disputer the moment `resolve()` lands and the stamp appears after it. | Money moves on evidence, on-chain, into an Agent Stack wallet; two numbers and the transition |
| 5 | 3:40 to 4:20 | The both-sides rule, against a web started in off-chain mode (`SIGIL_STAKE_ADDRESS= pnpm --filter @sigil/web exec next dev -p 3001`, same gateway): `/claims/new?skill=<cloud-helper>`, predicate `NO_ENV_READ_OUTSIDE`, allowlist `LOG_LEVEL`, stake `10`, `Mock Selfie Check (World Sandbox access pending)`, `Record claim (10.000000 USDC, off-chain)`, the step ledger to `POST /claims` `done` with `LIVE · topic 0.0.…`. On the redirected skill page click `Dispute` on that claim, load the example bundle, same address, same mock check: the step ledger's `POST /claims/:id/dispute` row reads `failed` with `409: one personhood proof cannot hold both sides of a claim`. Say why this shot is off-chain: on-chain the same wallet is refused one step earlier by `SigilStake.dispute()` (`own claim`), and the personhood rule needs two wallets with one proof, which the mock cannot produce. | World: personhood as abuse prevention, load-bearing |
| 6 | 4:20 to 4:50 | `/registry`: `cloud-helper` stamped BROKEN (`1 / 1` sustained in red, `1` under `paid`; the status column is derived, `sustainedDisputes > 0` wins over the shot-5 claim being LIVE), next to it `json-pretty` `RECORDED` with `0 / 1`: both outcomes on screen, a dispute that reproduced (`CLAIM_BROKEN`, disputer paid on Arc) and one that did not (`CLAIM_UPHELD`, staker kept the pot). Back on `/`, the live strip now reads `claims broken 1`, `paid requests 1` and `last settlement <resolve tx> on Arcscan`. Open the registry topic on HashScan, [0.0.10483153](https://hashscan.io/testnet/topic/0.0.10483153) (the top bar no longer links it): the trail (`SKILL_REGISTERED`, `CLAIM_OPENED`, the newest `IDENTITY`), then the shot-1 run's claim topic [0.0.10500327](https://hashscan.io/testnet/topic/0.0.10500327), five messages that carry the whole story: `CLAIM_OPENED` with the `openClaim` hash, `PAYMENT_SETTLED` with `asset 0.0.429274`, `amount 1000` and the settlement `txId`, `LICENSE_MINTED` serial 15, `DISPUTE_OPENED` with `by` = the Agent Stack wallet `0xf6ed…` and the `dispute` hash, `RESOLVED` with the `resolve()` hash and that wallet as `winner` (a fresh recording's topic carries the same five when T4 runs as e2e with the fresh payer; the earlier Circle-wallet run's topic [0.0.10499701](https://hashscan.io/testnet/topic/0.0.10499701) has three of them, `CLAIM_OPENED`, `DISPUTE_OPENED`, `RESOLVED`; the `demo-2` index's topic [0.0.10500947](https://hashscan.io/testnet/topic/0.0.10500947) carries all five again from the second USDC-leg run, `LICENSE_MINTED` serial 16, the `demo-3` index's topic [0.0.10502454](https://hashscan.io/testnet/topic/0.0.10502454) from the third, serial 17, and the `demo-4` index's topic [0.0.10503117](https://hashscan.io/testnet/topic/0.0.10503117) from the run through the hosted gateway, serial 22). | Verifiable audit trail on HCS; both outcomes |
| 7 | 4:50 to 5:00 | ARCHITECTURE.md Figure 1 and its deployed-addresses block. "Which bounty": Hedera AI & Agentic Payments; Arc Best Agentic Economy (Circle Agent Stack); World Selfie Check. | Submission hygiene |

Cut it once. Do not ramble. If a shot is a stub (mock World), the on-screen label says so; never hide it. If shot 1 prints `x402: paying the HBAR leg — agent holds no testnet USDC` the 2 USDC hand-off did not happen (agent account below 2 USDC, or the mirror node not yet showing the payer's balance): the payment is still real, but say which leg it was. T4 is recorded with `AGENT_WALLET_BACKEND=circle`: its `── wallet ──` line must read `circle (Agent Stack wallet)`; if it reads `LOCAL SIGNER (fallback)` the Circle session is missing, so stop and fix it before rolling. Between takes follow `Reset for another take` in RUN.md section 3: the pot is in the Circle wallet, so `circle wallet transfer` 10 USDC back to the deployer, fresh index, `SEED_RUN=demo-<n>`; licences persist on Hedera (the holders list is one longer each take) while the payments ledger is per index (`paid requests` and `paid` start at 0 again).
